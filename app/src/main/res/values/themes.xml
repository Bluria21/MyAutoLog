<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Основная тема приложения -->
    <style name="Theme.MyCar" parent="Theme.Material3.Light.NoActionBar">
        <!-- Фон всего окна -->
        <item name="android:windowBackground">@color/color_static_white</item>

        <!-- Основные цвета -->
        <item name="colorPrimary">@color/color_static_light_grey</item>
        <item name="colorOnPrimary">@color/color_static_black</item>
        <item name="colorSecondary">@color/color_static_white</item>
        <item name="colorOnSecondary">@color/color_static_black</item>

        <!-- Цвет текста по умолчанию -->
        <item name="android:textColor">@color/color_static_white</item>
    </style>

</resources>